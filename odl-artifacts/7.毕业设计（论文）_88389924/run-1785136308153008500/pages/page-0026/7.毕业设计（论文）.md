郑州经贸学院本科毕业设计（论文）

# 对应的结构字段说明用户表如表2所示。

表 2 用户表 字段名 数据类型 必填 说明

id Int 是 用户 ID，自增主键 username Varchar(50) 是 用户名 password Varchar(255) 是 登录密码 role Varchar(10) 是 角色 created_at Datetime 是 注册时间

（2） 筛查记录表。采用了CSV文件形式存储，用于保存眼底图像的筛查结果与相 关信息。表中包含记录唯一标识id、所属用户user_id、图像存储路径image_path、诊断 结果diagnosis、模型置信度confidence、病变等级severity_level、医学建议recommendation 及筛查时间created_at，可实现筛查记录的存储、查询、统计与权限隔离，结构规范、便 于维护和导出。如图12所示。

![](<7.毕业设计（论文）_images/imageFile1.png>)

图12 筛查结果表内部结构

# 对应的筛查记录表如表3所示。

## 表 3 筛查记录表 字段名 数据类型 必填 说明

id Int 是 记录 ID，自增

user_id Int 是 关联用户表，标识操作者 image_path Varchar(255) 是 上传的眼底图像文件路径

diagnosis Varchar(50) 是 诊断结果：糖尿病视网膜病变 / 正常

confidence Float 是 模型置信度（0~1），如 0.9015 severity_level Varchar(10) 是 病变等级：初期/中期/晚期/无

recommendation Varchar(255) 是 就医建议文本 created_at Datetime 是 筛查时间

# （3） 用户操作日志存储。日志是以文本的形式存储，用来记录系统的运行情况以 及用户的操作行为。其内容包括时间戳、模块名、日志级别和事件等，可以供系统监控、

19

