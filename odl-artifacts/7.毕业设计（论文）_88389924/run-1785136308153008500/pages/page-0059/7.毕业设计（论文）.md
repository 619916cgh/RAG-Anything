郑州经贸学院本科毕业设计（论文）

file_lock = threading.Lock() # 用户数据文件路径 USERS_JSON = 'users.json' # 角色定义 ROLE_ADMIN = 'admin' ROLE_USER = 'user' # 初始化用户数据文件 def init_users_file():

"""初始化用户数据文件，如果不存在则创建，并创建默认管理员账户""" if not os.path.exists(USERS_JSON):

# 创建默认管理员账户 default_admin = {

'id': 1, 'username': 'root', 'password': generate_password_hash('root'), # 默认密码：root 'role': ROLE_ADMIN, 'created_at': datetime.now().strftime('%Y-%m-%d %H:%M:%S')

} with open(USERS_JSON, 'w', encoding='utf-8') as f:

json.dump([default_admin], f, ensure_ascii=False, indent=4) logger.info("已创建默认管理员账户: root/root")

else:

# 检查现有用户是否有角色字段，如果没有则添加（默认普通用户） try:

users = read_users() updated = False for user in users:

if 'role' not in user: user['role'] = ROLE_USER updated = True

# 检查是否存在管理员账户，如果没有则创建默认管理员 has_admin = any(user.get('role') == ROLE_ADMIN for user in users) if not has_admin:

max_id = max([u.get('id', 0) for u in users]) if users else 0 default_admin = {

'id': max_id + 1, 'username': 'root', 'password': generate_password_hash('root'), 'role': ROLE_ADMIN, 'created_at': datetime.now().strftime('%Y-%m-%d %H:%M:%S')

} users.append(default_admin) updated = True logger.info("已添加默认管理员账户: root/root")

if updated:

save_users(users) except Exception as e:

logger.error(f"初始化用户角色失败: {str(e)}")

if 'user_id' in session: session.permanent = True # 设置会话为永久，使用配置的过期时间 return render_template('index.html', error='', is_admin=is_admin(),

52

