郑州经贸学院本科毕业设计（论文）

- （3） 特征提取。从预处理后的图像中提取有用的特征。在深度学习出现之前，这

通常需要手动设计特征，如 SIFT、HOG 等。深度学习使得自动特征提取成为可能。

- （4） 模型训练。使用提取的特征来训练分类器。这可以是一个传统的机器学习算

法，如支持向量机或随机森林，也可以是一个深度学习模型，如卷积神经网络。

- （5） 模型评估。通过在未见过的图像上测试模型的性能来评估其准确性。常用的

评估指标包括准确率、召回率、精确率和 F1 分数。

- （6） 模型部署。将训练好的模型部署到实际应用中，用于对实时或新的图像进行


分类。

深度学习的出现极大地推动了图像分类技术的发展。特别是卷积神经网络即 CNN 在图像分类任务中取得了显著的成功。CNN 能够自动学习图像的层次化特征表示，从 而提高了分类的准确性。一些著名的 CNN 架构，如 AlexNet、VGG、GoogLeNet（Inception）、

MobileNetV3 和 DenseNet，都在图像分类任务中取得了突破性的成果。图像分类是一个 不断发展的领域，随着技术的进步和数据的积累，图像分类系统将更加准确、鲁棒和可 靠。

# 2.2.2 卷积神经网络结构

卷积神经网络是一种结构化的深度学习模型，它巧妙地融合了卷积层、池化层和输 出层，以实现对图像原始像素的高效处理。这种网络架构通过逐层提取和抽象图像特征， 最终将高维的视觉信息映射到具体的输出类别上。卷积神经网络的一般结构图如图 1 所 示。

![](<7.毕业设计（论文）_images/imageFile1.png>)

图1 卷积神经网络的基本结构

该图给出了卷积神经网络的基本结构：输入图像经多层卷积层提取特征、池化层降 维后，由全连接层输出分类结果，实现了端到端的自动图像分类。

（1） 卷积层。在 CNN 中，卷积层扮演着至关重要的角色，它是特征提取的引擎。

卷积层通过一系列卷积运算，将输入图像与多个学习得到的卷积核进行交互，从而捕捉 到图像中的各种特征模式。这些特征可能包括边缘、角点、纹理等低级视觉特征，也可

6

