郑州经贸学院本科毕业设计（论文）

filepath = os.path.join(app.config['UPLOAD_FOLDER'], unique_filename) # 保存图片 file.save(filepath) # 使用预测函数，返回诊断结果、置信度和病变等级 diagnosis, confidence, severity_level = p1(filepath) # 生成完整的诊断结果和建议 if diagnosis == "糖尿病视网膜病变":

full_diagnosis = f"{diagnosis}-{severity_level}" else:

full_diagnosis = diagnosis recommendation = DR_SEVERITY_INFO.get(full_diagnosis, '请咨询医生获取详细建议') # 保存到 CSV（加锁防止并发问题） with file_lock:

try:

df = pd.read_csv(RESULTS_CSV) if os.path.exists(RESULTS_CSV) else pd.DataFrame(columns=['id', 'user_id', 'image_path', 'diagnosis', 'confidence', 'severity_level', 'recommendation', 'created_at'])

54

