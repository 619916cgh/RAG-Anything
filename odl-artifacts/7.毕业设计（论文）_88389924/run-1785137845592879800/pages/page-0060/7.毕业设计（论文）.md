郑州经贸学院本科毕业设计（论文）

username=session.get('username', '')) return redirect(url_for('login'))

@app.route("/help", methods=['POST', 'GET']) def help():

return render_template('help.html')

@app.route('/single-detection') def single_detection():

"""单张识别页面""" # 检查用户是否已登录 if 'user_id' not in session:

return redirect(url_for('login')) return render_template('single_detection.html', is_admin=is_admin())

@app.route('/batch-detection') def batch_detection():

"""批量识别页面"""

# 检查用户是否已登录 if 'user_id' not in session:

return redirect(url_for('login')) return render_template('batch_detection.html', is_admin=is_admin())

@app.route('/history') def history():

"""历史记录页面"""

# 检查用户是否已登录 if 'user_id' not in session:

return redirect(url_for('login')) return render_template('history.html', is_admin=is_admin())

@app.route('/process-single-detection', methods=['POST']) def process_single_detection():

"""处理单张图片识别请求"""

# 检查用户是否已登录 if 'user_id' not in session:

return jsonify({'error': '请先登录'}), 401 try:

if 'file' not in request.files: return jsonify({'error': '请上传图片'}), 400

file = request.files['file'] if file.filename == '':

return jsonify({'error': '请选择图片'}), 400

# 验证文件类型 if not allowed_file(file.filename):

return jsonify({'error': '不支持的文件格式，请上传 jpg、jpeg 或 png 格式的图片'}), 400

# 生成唯一的文件名，避免冲突 filename = secure_filename(file.filename) name, ext = os.path.splitext(filename) unique_filename = f"{name}_{uuid.uuid4().hex[:8]}{ext}"

53

